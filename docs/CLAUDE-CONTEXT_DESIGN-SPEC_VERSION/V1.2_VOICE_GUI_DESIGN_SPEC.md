# V1.2 — Voice GUI Context

## Purpose

Implement **v1.2 — Voice GUI** by exposing the existing v0.4 Voice System through the existing desktop GUI.

This is a **GUI integration milestone**, not Voice System v2.

Do not rebuild or redesign:
- Recorder
- SpeechToText
- TextToSpeech
- AudioPlayer
- VoiceManager
- Companion
- GeminiClient
- the existing voice pipeline

The repository's actual implementation is the source of truth.

---

## 1. Current State

Completed:

- v0.1 — Chat with Gemini
- v0.2 — Desktop GUI
- v0.3 — SQLite Memory
- v0.4 — Voice / STT / TTS
- v0.5 — VTube Studio / Avatar
- v0.6.1 — Idle Animation
- v0.6.2 — Emotion System
- v0.6.3 — Relationship System
- v0.6.4 — Internal State
- v0.6.5 — Behavior Integration
- v0.7 — Vision
- v0.8 — Routine
- v0.9 — Initiative
- v0.9.5 — Developer Tools
- v1.0 — Stable Release
- v1.1 — Memory GUI

v1.0 GUI has been tested and is working.

v1.1 Memory GUI has been completed.

The sidebar currently has Voice marked as **Coming Soon**.

v1.2 replaces that placeholder with a real Voice page.

---

# 2. Primary Objective

Teacher must be able to:

1. Open Voice from the sidebar.
2. See Voice System status.
3. Start/stop microphone recording using the existing Recorder/VoiceManager API.
4. Send recorded audio through the existing STT system.
5. Display recognized text.
6. Send recognized text through the existing Companion chat path.
7. Display Arona's response.
8. Send the response through the existing TTS system.
9. Play generated audio using the existing AudioPlayer.
10. See loading, processing, speaking, and error states.
11. Keep the GUI responsive.

The exact method names and contracts MUST be taken from the repository after inspection.

---

# 3. Golden Rule

> **Inspect first. Implement second. Test third. Document last.**

Before changing anything, inspect:

- `speech/`
- `ui/`
- `ai/companion.py`
- `ai/gemini.py`
- `main_gui.py`
- `main.py`
- `config/constants.py`
- `config/settings.py`
- existing `ui/*_worker.py`
- Recorder
- SpeechToText
- TextToSpeech
- AudioPlayer
- VoiceManager
- GUI navigation
- current page architecture
- existing tests
- v1.1 documentation/context

Do not assume an API exists because this document uses a conceptual name.

---

# 4. Existing Voice Architecture

The v0.4 Voice System already contains the conceptual components:

```text
Recorder
SpeechToText
TextToSpeech
AudioPlayer
VoiceManager
```

Conceptual flow:

```text
Microphone
    ↓
Recorder
    ↓
SpeechToText
    ↓
recognized text
    ↓
Companion.chat()
    ↓
Arona response
    ↓
TextToSpeech
    ↓
AudioPlayer
```

The GUI is only the presentation/control layer.

Do not duplicate any of these systems.

---

# 5. Architecture Rules

The intended relationship is:

```text
Voice GUI
    ↓
existing VoiceManager / existing Voice API
    ↓
Recorder / STT / TTS / AudioPlayer
    ↓
Companion
```

The GUI must NOT implement:

- microphone capture internals
- STT internals
- TTS generation
- audio decoding
- audio playback internals
- Gemini API requests
- voice orchestration

Those responsibilities remain in the existing backend.

---

# 6. UI Independence

Existing rule:

> **Qt imports belong only inside `ui/`.**

Backend modules must not import Qt.

Correct:

```text
ui/
   voice.py
   voice_worker.py
        ↓
VoiceManager
        ↓
speech/
```

Incorrect:

```text
speech/
   recorder.py
      ↓
   PySide6
```

Do not move backend logic into `ui/`.

---

# 7. Scope

## Included

- Voice navigation entry.
- Voice page.
- Voice status.
- Recording control.
- STT state.
- Recognized text display.
- Arona response display.
- TTS state.
- Playback state.
- Error states.
- Loading/busy states.
- Responsive GUI integration.
- Reuse of existing workers.
- Tests.
- Documentation updates.

## Explicitly excluded

- Voice System v2.
- New STT engine.
- New TTS engine.
- New audio engine.
- New microphone library.
- New Gemini client.
- New VoiceManager.
- Voice cloning.
- Voice training.
- Wake-word system.
- Always-listening mode.
- Discord voice.
- Web voice.
- Plugin System.
- Outfit System.
- Avatar GUI.
- Settings GUI.

---

# 8. Existing v0.4 Contracts

Inspect the actual implementations before coding.

## Recorder

Determine:
- start behavior
- stop behavior
- return type
- recording state
- errors
- worker/thread behavior

## SpeechToText

Determine:
- input type
- output type
- provider/model
- language handling
- errors
- synchronous/asynchronous behavior

## TextToSpeech

Determine:
- input text
- output format
- model
- voice parameter
- error behavior
- preprocessing

## AudioPlayer

Determine:
- audio input
- playback behavior
- stop behavior
- state reporting
- threading

## VoiceManager

Determine:
- orchestration responsibilities
- public API
- callbacks/signals
- state model
- error model

Do not redesign these contracts.

---

# 9. TTS Configuration

Current configuration includes:

```python
TTS_MODEL_NAME = "gemini-2.5-flash-tts"
```

Preserve the existing configuration.

Do not hard-code the model in GUI code.

Use the existing TextToSpeech implementation and configuration.

---

# 10. Historical v0.4 Bugs — Must Not Return

### Incorrect TTS model name

Use the existing configured model:

```text
gemini-2.5-flash-tts
```

Do not invent another identifier.

### Stage directions spoken aloud

If Arona responds with:

```text
(Smiles softly)

Teacher, welcome back!
```

the GUI may display the full response, but TTS must preserve the existing preprocessing that prevents stage directions from being spoken accidentally.

Do NOT move this preprocessing into the GUI.

### Incorrect date/time assumptions

Do not introduce unrelated date/time logic into Voice GUI.

### Incorrect `voice_name` parameter

Use the exact existing TextToSpeech contract.

Do not rename it to another invented parameter.

---

# 11. GUI Design

Follow the existing v1.0/v1.1 visual language.

Do not redesign the application.

Suggested conceptual layout:

```text
┌─────────────────────────────────────────────────────────────┐
│ Voice                                                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│ Voice System                                                │
│ ● Ready                                                     │
│                                                             │
│                    [ 🎙 Start Recording ]                   │
│                    [ Stop Recording ]                       │
│                                                             │
│ Status: Ready                                               │
│                                                             │
│ You said:                                                   │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Hello Arona...                                          │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ Arona:                                                      │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ Ehehe~ Hello, Teacher!                                  │ │
│ └─────────────────────────────────────────────────────────┘ │
│                                                             │
│ TTS: ● Ready       Audio: ● Idle                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

Exact visual details may follow the existing design system.

Architecture is not flexible.

---

# 12. Voice States

Expose meaningful states such as:

```text
Idle
Recording
Transcribing
Thinking
Speaking
Error
```

BUT:

> Do not create a second competing backend state machine.

If VoiceManager already exposes suitable states, map those into GUI presentation.

A UI-only mapping is acceptable.

---

# 13. Recording Flow

Target:

```text
Teacher clicks Start
        ↓
Voice GUI
        ↓
existing VoiceManager / Recorder
        ↓
Recording
        ↓
Teacher stops
        ↓
STT
        ↓
recognized text
        ↓
display text
```

The GUI must not directly manage low-level microphone resources unless the existing API explicitly requires it.

---

# 14. Shared Companion Path

Voice and text must use the same logical Companion.

Conceptually:

```text
Typed Input ───────────┐
                       ↓
                    Companion
                       ↓
                  Arona response
                       ↓
                 optional TTS
```

and:

```text
Voice
 ↓
STT
 ↓
Text
 ↓
Companion
 ↓
Arona response
```

Do not create:

```text
VoiceCompanion
TextCompanion
```

There must remain one logical conversation/Companion.

---

# 15. TTS Flow

After receiving Arona's response:

```text
reply
 ↓
existing TextToSpeech
 ↓
audio
 ↓
existing AudioPlayer
```

The GUI must NOT:

- call Gemini directly
- create a Gemini client
- generate TTS itself
- duplicate TTS preprocessing
- bypass VoiceManager when the existing architecture provides a suitable boundary

---

# 16. Worker / Threading Rules

Potentially blocking operations:

- recording
- STT
- Companion/Gemini
- TTS
- audio playback

The GUI must remain responsive.

First inspect the existing v0.4 worker architecture.

Reuse existing `ui/*_worker.py` infrastructure.

Do not create a new threading architecture merely because a page needs a loading state.

If a new worker is genuinely required, follow the existing worker pattern.

Do not duplicate workers.

---

# 17. Error Handling

User-facing errors should be concise.

Examples:

```text
Microphone unavailable.

Please check your microphone.
```

```text
Speech recognition failed.

Please try again.
```

```text
Voice generation failed.

Please try again.
```

Technical details belong in the existing logging system.

Do not expose stack traces in normal GUI operation.

---

# 18. Gemini 429 / Quota

Gemini may return quota/API errors.

If a 429 occurs:

- GUI must not crash.
- Existing error handling must remain intact.
- Show a friendly state/message.
- Log technical information using the existing logging system.

Do not create a second quota-management system.

---

# 19. Optional Controls

Potential controls:

```text
[ Start Recording ]
[ Stop Recording ]
[ Stop Playback ]
[ Retry ]
```

Only implement controls supported by the real backend.

If AudioPlayer has no stop API, do not invent one.

If Recorder already provides toggle behavior, use it instead of inventing another model.

---

# 20. Voice Settings

Do NOT turn Voice GUI into Settings GUI.

Microphone/device/TTS voice configuration should only be exposed if the existing configuration architecture already supports it cleanly.

Otherwise leave configuration for the future Settings GUI.

Do not create a temporary settings system inside Voice GUI.

---

# 21. No Direct Hardware Logic

The GUI must not:

- enumerate microphones directly
- open audio devices directly
- manage audio buffers
- decode audio
- manage Gemini credentials

Use existing backend services.

---

# 22. No Avatar Coupling

Voice GUI must not own avatar reactions.

The existing architecture should remain conceptually:

```text
Companion response
       │
       ├────────→ TTS
       │
       └────────→ Avatar System
```

Voice GUI must not call HaloMapper, ParameterMapper, Idle, Expression, etc. unless the current backend contract explicitly requires it.

Avatar GUI is a separate future milestone.

---

# 23. No Behavior Coupling

Voice GUI must not directly manipulate:

- Emotion
- Relationship
- Mood
- Energy
- Curiosity
- Initiative
- Routine

Those systems remain controlled by the existing application architecture.

---

# 24. Navigation

Replace:

```text
Voice · Coming Soon
```

with:

```text
Voice
```

Verify:

- Voice page opens.
- Chat remains functional.
- Memory remains functional.
- Existing sidebar behavior remains intact.
- No unrelated navigation redesign occurs.

---

# 25. File/Structure Policy

Potential files may include:

```text
ui/
├── voice.py
├── voice_worker.py
```

BUT:

> These are only examples.

First inspect the current repository.

If an equivalent page/worker already exists, reuse it.

Do not create duplicate modules.

---

# 26. Implementation Sequence

## Stage 1 — Inspect

Do not modify code.

Inspect:

- `speech/`
- `ui/`
- VoiceManager
- Recorder
- SpeechToText
- TextToSpeech
- AudioPlayer
- Companion
- main_gui
- navigation
- workers
- configuration
- tests
- v1.1 implementation

Record actual contracts.

## Stage 2 — Map

Map the real architecture:

```text
GUI
 ↓
existing Voice API
 ↓
Recorder / STT
 ↓
Companion
 ↓
TTS
 ↓
AudioPlayer
```

Identify the smallest correct integration points.

## Stage 3 — Navigation

Activate Voice navigation.

Verify Chat and Memory still work.

## Stage 4 — Voice Page

Implement:

- status
- recording
- recognized text
- Arona response
- TTS status
- playback status

## Stage 5 — Worker Integration

Reuse existing worker architecture.

## Stage 6 — End-to-End

Verify:

```text
Microphone
 ↓
Recorder
 ↓
STT
 ↓
Companion
 ↓
Arona
 ↓
TTS
 ↓
AudioPlayer
```

## Stage 7 — Error Testing

Test:

- microphone unavailable
- recording failure
- empty transcription
- STT failure
- Gemini failure
- Gemini 429
- TTS failure
- playback failure

## Stage 8 — Regression

Verify:

- Chat
- Memory
- Voice
- Avatar
- Vision
- Routine
- Initiative
- Developer Tools

## Stage 9 — Documentation

After implementation is verified, update only relevant:

- `CHANGELOG.md`
- `ARCHITECTURE.md`
- `ROADMAP.md`
- `TECH_STACK.md`

Do not document features that were not implemented.

---

# 27. Acceptance Criteria

## Navigation

- [ ] Voice no longer says "Coming Soon".
- [ ] Voice page opens.
- [ ] Chat works.
- [ ] Memory works.
- [ ] Navigation remains intact.

## Recording

- [ ] Start recording works.
- [ ] Stop recording works when supported.
- [ ] Recording state is visible.
- [ ] GUI remains responsive.
- [ ] Microphone errors are handled.

## STT

- [ ] Existing Recorder/STT pipeline is used.
- [ ] Recognized text is displayed.
- [ ] Empty transcription is handled.
- [ ] STT errors are handled.

## Companion

- [ ] Voice input reaches the same logical Companion path as text.
- [ ] No duplicate AI conversation engine exists.
- [ ] Conversation history remains correct.
- [ ] Existing behavior systems remain intact.

## TTS

- [ ] Arona response reaches existing TTS.
- [ ] `TTS_MODEL_NAME` configuration remains authoritative.
- [ ] Existing `voice_name` contract remains intact.
- [ ] Stage directions are not accidentally spoken.
- [ ] TTS errors are handled.

## Playback

- [ ] Generated audio reaches existing AudioPlayer.
- [ ] Playback status is visible.
- [ ] Playback does not freeze the GUI.
- [ ] Stop playback works only if supported.

## Architecture

- [ ] GUI does not implement microphone logic.
- [ ] GUI does not implement STT.
- [ ] GUI does not implement TTS.
- [ ] GUI does not implement audio decoding/playback.
- [ ] GUI does not call Gemini directly.
- [ ] Backend does not import Qt.
- [ ] No duplicate VoiceManager.
- [ ] No duplicate worker.
- [ ] No duplicate voice pipeline.

## Regression

- [ ] Chat works.
- [ ] Memory works.
- [ ] Avatar works.
- [ ] Vision works.
- [ ] Routine works.
- [ ] Initiative works.
- [ ] Developer Tools work.

---

# 28. Definition of Done

```text
Voice GUI
  +-- Navigation       OK
  +-- Status           OK
  +-- Recording        OK
  +-- STT              OK
  +-- Companion        OK
  +-- TTS              OK
  +-- Playback         OK
  +-- Error states     OK
  +-- Responsive       OK

Architecture
  +-- Existing Voice System reused
  +-- No duplicate pipeline
  +-- No direct Gemini from GUI
  +-- Qt isolation preserved
  +-- No Avatar coupling
  +-- No Behavior coupling

Regression
  +-- Existing systems remain functional
```

---

# 29. Stop Conditions

Claude MUST stop and ask for clarification rather than guessing if:

1. Existing VoiceManager contracts contradict this document.
2. Existing TTS behavior differs from the assumptions here.
3. Existing STT architecture requires a new architectural decision.
4. A new dependency appears necessary.
5. Companion must be substantially redesigned.
6. A database change appears necessary.
7. Avatar architecture must be changed.
8. Existing workers cannot safely support the GUI flow without architectural redesign.
9. Implementation would become Voice System v2.
10. A requirement cannot be implemented without violating an established project policy.

Do not silently make architectural decisions.

---

# 30. Forbidden Changes

Do NOT introduce:

- Plugin System
- Outfit System
- Voice System v2
- new AI model
- new database
- new Memory architecture
- new Companion
- new VoiceManager
- new AvatarManager
- new Emotion System
- new Behavior Engine
- new autonomous system
- new persistence layer
- new GUI framework

Do not refactor stable systems merely to make v1.2 easier.

---

# 31. Design Philosophy

The desktop GUI is:

> **Presentation + User Control**

The Voice System is:

> **Voice Infrastructure**

The Companion is:

> **Application/AI Orchestrator**

Therefore:

```text
Voice GUI
     ↓
Existing Voice System
     ↓
Companion
```

not:

```text
Voice GUI
     ↓
New Voice Logic
     ↓
New Gemini Client
     ↓
New Companion
```

---

# 32. Final Instruction to Claude

You are implementing:

> **v1.2 — Voice GUI**

You are NOT rebuilding the Voice System.

You are NOT redesigning Companion.

You are NOT creating a second speech pipeline.

You are exposing the already-working v0.4 Voice System through the already-working v1.x GUI.

Priority:

1. Inspect actual repository.
2. Preserve existing contracts.
3. Reuse existing Voice infrastructure.
4. Reuse existing worker infrastructure.
5. Make the smallest safe GUI changes.
6. Keep GUI responsive.
7. Preserve STT/TTS behavior.
8. Preserve subsystem boundaries.
9. Test end-to-end.
10. Run regression tests.
11. Update documentation only after verification.

Never guess an API.

Never duplicate an existing class.

Never create a second worker when one already exists.

Never call Gemini directly from Voice GUI.

Never put Qt into backend modules.

Never move Voice responsibilities into the GUI.

Never introduce Plugin System.

Never introduce Outfit System.

> **Inspect first. Reuse second. Implement minimally. Test thoroughly. Document last.**

---

# 33. Expected Result

After v1.2:

```text
ARONA

+-- Chat
+-- Memory
+-- Voice                 ← implemented
+-- Avatar · Coming Soon
+-- Settings · Coming Soon
```

Teacher can interact with Arona through the microphone from the desktop GUI, receive the same Companion response path used by normal text interaction, hear Arona through the existing Gemini TTS system, and observe relevant Voice System states without the GUI taking ownership of the underlying voice architecture.

---

# End of V1.2 Voice GUI Context
